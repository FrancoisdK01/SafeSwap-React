Go Live => Config Netlify Variables in https://app.netlify.com/sites/your-site-url/configuration/env#content

PAYFAST_PASSPHRASE = ***
NODE_ENV = production 

===========================
? What will this do ?
===========================

paymentNotify.Js

line 42
const check1 = validateSignature(payload, pfParamString, process.env.PAYFAST_PASSPHRASE);

line 129  
const pfHost = process.env.NODE_ENV === 'production' ? 'www.payfast.co.za' : 'sandbox.payfast.co.za';
